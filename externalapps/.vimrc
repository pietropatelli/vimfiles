" NOTE: This is the .vimrc file that must be in the home directory; the
" actual vim configuration is in one of the locations this file points to.
if has('win32')
    source ~/vimfiles/vimrc
else
    source ~/.vim/vimrc
endif
